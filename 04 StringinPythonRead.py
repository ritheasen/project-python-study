name = input("Input your name:")
print(name)

# extracting the first character of a string
print(f"he first character of the string enterned: {name[0]}")

# extracting the last character of a string
print(f"The first character of the string enterned: {name[len(name)-1]}")

# extracting the first character of a string using negative indexing
print(f"The first character of the string enterned: {name[-len(name)]}")
#Extracting the last character of the string using negative indexing
print(f"The last character of the string entered: {name[-1]}")

